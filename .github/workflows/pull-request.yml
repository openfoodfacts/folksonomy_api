name: Pull Request checks

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-tests:
    name: Run pytest tests
    runs-on: ubuntu-latest
    steps:
      - name: checkout project
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: start a postgresql docker container
        run: ./start_postgres.sh &
      - name: install python
        uses: actions/setup-python@v4.7.1
        with:
          python-version: '3.8'
          cache: 'pip'
      - name: install packages in a virtual environment
        run: |
          python -m venv .venv && \
          .venv/bin/pip install -r requirements.txt
      - name: create local settings for tests
        run: |
          echo > local_settings.py
          echo "POSTGRES_USER = 'folksonomy'" >> local_settings.py
          echo "POSTGRES_PASSWORD = 'folksonomy'" >> local_settings.py
          echo "POSTGRES_HOST = 'localhost'" >> local_settings.py
          echo "POSTGRES_DATABASE = 'folksonomy'" >> local_settings.py
      - name: wait postgres to be ready
        timeout-minutes: 10
        run: |
          while ! (nc -vz 127.0.0.1 5432);do echo "waiting postgres"; sleep 1;done
      - name: initialize database
        run: |
          .venv/bin/python db-migration.py
      - name: run tests
        # use PYTHONASYNCIODEBUG to ensure all asyncio tasks are closed
        run: |
          PYTHONASYNCIODEBUG=1 .venv/bin/pytest tests/
